// Reverse the elements in an array.
var numbers=[0,1,2,3,4,5]
numbers.reverse();// reverse method is used to reverse the array elements from end to start
console.log(numbers);//output :[5, 4, 3, 2, 1, 0]

var char=["A","B","C"]
char.reverse()
console.log(char);// output: ["C", "B", "A"]

        //reverse() method alter the original array