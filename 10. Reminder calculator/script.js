//Write a program that calculates the remainder when one number is divided by another.
var dividend = 16;//dividend/divisor
var divisor = 2;
var remainder = dividend % divisor;
if(divisor===0){
    console.log("0 can not be divided by any number");
    
}
else{console.log("The remainder is " + remainder + " when " + dividend + " is devided by " + divisor);}
