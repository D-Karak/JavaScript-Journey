//Create a sub-array from an array (slice).

const arr=[1,2,3,4,5]
const subArr=arr.slice(2,4);// slice method sliced out a piece of array into a new array by elements index no.
                            //slice(starting index no., ending index no.) the ending index element will exclude
console.log(subArr);// output: [3,4]